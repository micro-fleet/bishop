const { test } = require('ava')

const bishop = require(`${process.env.PWD}/src`)()

test.todo('add pattern')

test.todo('use plugin')

test.todo('act nonexisting pattern')

test.todo('act existing pattern')
