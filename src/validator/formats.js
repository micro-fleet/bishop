// const semver = require('semver')

module.exports = {
  // servicename: '^[a-z0-9-_]+$',
  // transportname: '^[a-z0-9-_]+$',
  // taskname: '^[a-z0-9-_.]+$',
  // semver: version => semver.valid(version)
}
